<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 160 28" fill="none">
  <!-- Replace this file with your custom logo SVG -->
  <!-- Use fill="currentColor" on paths so the logo inherits the page text color -->
  <text x="0" y="21" font-family="Georgia, serif" font-size="16" letter-spacing="0.04em" fill="currentColor">Messy Sensing</text>
</svg>
